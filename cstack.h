#ifndef CSTACK_H
#define CSTACK_H

//Опишем нашу задачу. 
/*
1.Необходимо реализовать функции связанные с работой стека
2.Создать библиотеку
3.По возможности проверить с помощью gtest. Пока не понятно, с таким не работал
*/

typedef int hstack_t;
// hstack_t - тип дескриптора стека (handle stack type я так понимаю)
// это не указатель, а числовой идентификатор, который мы будем использовать
// для доступа к реальным данным стека через внутреннюю структуру


hstack_t stack_new(void);
// Создает пользовательский стек и возвращает его в дискриптор.
// возвращение -1 при ошибке


void stack_free(const hstack_t stack);
// Удаляет стек по его дескриптору и освобождает всю связанную с ним память
// const hstack_t - дескриптор передается по значению, 
// но не может быть изменен внутри функции (хотя это и так невозможно для int)

int stack_valid_handler(const hstack_t stack);
// Проверяет валидность дескриптора стека
// Возвращает 1 если дескриптор валиден, 0 - если нет

unsigned int stack_size(const hstack_t stack);
// Возвращает количество элементов в стеке
// Для невалидного дескриптора возвращает 0

void stack_push(const hstack_t stack, const void* data_in, const unsigned int size);
// Добавляет элемент в стек
// data_in - указатель на данные для добавления
// size - размер данных в байтах
// const void* data_in - указатель на любые данные, 
// которые нельзя изменить внутри этой функции

unsigned int stack_pop(const hstack_t stack, void* data_out, const unsigned int size);
// Извлекает элемент из стека
// data_out - буфер для записи извлеченных данных
// size - ожидаемый размер данных в байтах
// Возвращает количество фактически записанных байт
// void* data_out - указатель на буфер любого типа, 
// куда будут записаны данные

#endif /* CSTACK_H */
